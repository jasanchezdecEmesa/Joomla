<?xml version="1.0" encoding="utf-8"?>
<form>
    <fields name="filter">
        <field
            name="search"
            type="text"
            label="Filter by Name"
            placeholder="Search by first or last name"
            onchange="this.form.submit();"
        />

        <field
            name="nationality"
            type="sql"
            label="Filter by Nationality"
            onchange="this.form.submit();"
            query="SELECT DISTINCT nationality AS value, nationality AS text FROM #__formula1_drivers WHERE nationality != '' ORDER BY nationality ASC"
            key_field="value"
            value_field="text"
        >
            <option value="">- Select Nationality -</option>
        </field>
    </fields>
</form>